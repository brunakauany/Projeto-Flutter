name: jogo_da_velha

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Ícones padrão
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Boas práticas de código
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/images/